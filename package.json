{
  "name": "@mtngtools/hls",
  "version": "0.1.0",
  "private": true,
  "description": "HLS (HTTP Live Streaming) utilities monorepo",
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run typecheck",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "prepublishOnly": "pnpm run typecheck && pnpm run test:typecheck && pnpm run lint && pnpm run build && pnpm run test",
    "test:typecheck": "turbo run test:typecheck",
    "publish:experimental": "pnpm -r run publish:experimental"
  },
  "keywords": [
    "hls",
    "http-live-streaming",
    "m3u8",
    "streaming",
    "video"
  ],
  "author": "mtngtools",
  "license": "MIT",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "devDependencies": {
    "@turbo/gen": "^2.0.0",
    "msw": "^2.0.0",
    "oxlint": "^1.49.0",
    "rimraf": "^6.1.2",
    "turbo": "^2.0.0",
    "typescript": "^5.7.0",
    "vite": "8.0.0-beta.15",
    "vite-plugin-dts": "^4.5.4",
    "vitest": "^2.0.0"
  }
}
